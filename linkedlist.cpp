#include<bits/stdc++.h>
using namespace std;

struct Node{
    int data;
    struct node *next;
};

int main(){
    Node* head;
    Node* tail;

    head


    while(current != NULL){
        cout<<current->data;
        current = current -> next;  
    }
    
}
